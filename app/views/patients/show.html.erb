<% @patient.appointments.each do |appointment| %>
  <div><%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
  <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></div>
<% end %>
